{
  "name": "tool-ep",
  "private": true,
  "scripts": {
    "build": "charge build src public",
    "dev": "charge serve src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@static/charge": "^1.7.0"
  }
}
